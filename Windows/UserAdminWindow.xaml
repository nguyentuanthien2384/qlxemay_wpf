<UserControl x:Class="QLXeMay.Windows.UserAdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        FontFamily="Segoe UI"
        Background="#F1F5F9">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PanelBlue" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="Pink" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PinkDark" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="Line" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="Ink" Color="#0F172A"/>
        <SolidColorBrush x:Key="Muted" Color="#64748B"/>
        <Style x:Key="CommandButton" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="92"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource CommandButton}">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#1D4ED8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="Input" TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
        <Style x:Key="PasswordInput" TargetType="{x:Type PasswordBox}">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="176"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource Pink}" BorderBrush="#1D4ED8" BorderThickness="0,0,1,0">
            <DockPanel Margin="12">
                <StackPanel DockPanel.Dock="Top">
                    <Border Width="70" Height="70" CornerRadius="35" Background="#FFFFFF" BorderBrush="#333333" BorderThickness="1" HorizontalAlignment="Center">
                        <TextBlock Text="🏍" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Chợ mua xe máy" FontStyle="Italic" FontWeight="Bold" TextAlignment="Center" Margin="0,10,0,0"/>
                    <TextBlock Text="Admin Console" TextAlignment="Center" Foreground="{StaticResource Muted}" FontSize="12"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom">
                    <Button Content="Làm mới" Command="{Binding RefreshCommand}" Style="{StaticResource CommandButton}" Margin="0,0,0,8"/>
                    <Button Content="Đóng" Command="{Binding CloseCommand}" Style="{StaticResource CommandButton}" Margin="0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Danh sách tài khoản" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Ink}" HorizontalAlignment="Center"/>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="340"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="16">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Thông tin tài khoản" FontSize="18" FontWeight="Bold"/>
                            <TextBlock Text="Tạo mới, cập nhật quyền, reset mật khẩu hoặc mở khóa tài khoản." Foreground="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,4,0,16"/>

                            <TextBlock Text="Tên đăng nhập" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding NewUserName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Input}"/>

                            <TextBlock Text="Tên hiển thị" FontWeight="SemiBold" Margin="0,10,0,4"/>
                            <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Input}"/>

                            <TextBlock Text="Vai trò" FontWeight="SemiBold" Margin="0,10,0,4"/>
                            <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding SelectedRole, Mode=TwoWay}" DisplayMemberPath="DisplayName" Height="32"/>

                            <TextBlock Text="Mật khẩu / Mật khẩu tạm" FontWeight="SemiBold" Margin="0,10,0,4"/>
                            <PasswordBox x:Name="PasswordInput" PasswordChanged="PasswordInput_PasswordChanged" Style="{StaticResource PasswordInput}"/>

                            <CheckBox Content="Tài khoản hoạt động" IsChecked="{Binding IsActive, Mode=TwoWay}" Margin="0,12,0,0"/>

                            <Border Background="#FFFFFF" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="10" Margin="0,14,0,0">
                                <TextBlock Text="Mật khẩu tối thiểu 10 ký tự, có chữ hoa, chữ thường, số và ký tự đặc biệt. Reset mật khẩu sẽ buộc người dùng đổi mật khẩu ở lần đăng nhập tiếp theo." TextWrapping="Wrap" Foreground="{StaticResource Muted}"/>
                            </Border>

                            <TextBlock Text="{Binding SelectedUserSummary}" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,14,0,0" Foreground="#1E40AF"/>

                            <WrapPanel Margin="0,16,0,0">
                                <Button Content="Thêm" Command="{Binding CreateUserCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,6,8"/>
                                <Button Content="Sửa" Command="{Binding UpdateUserCommand}" Style="{StaticResource CommandButton}" Margin="0,0,6,8"/>
                                <Button Content="Reset" Command="{Binding ResetPasswordCommand}" Style="{StaticResource CommandButton}" Margin="0,0,6,8"/>
                                <Button Content="Mở khóa" Command="{Binding UnlockUserCommand}" Style="{StaticResource CommandButton}" Margin="0,0,6,8"/>
                                <Button Content="Xóa form" Command="{Binding ClearCommand}" Style="{StaticResource CommandButton}" Margin="0,0,6,8"/>
                            </WrapPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <Border Grid.Column="2" Background="#F8FAFC" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="12">
                    <DataGrid ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="All"
                              AlternatingRowBackground="#F8FAFC"
                              Background="#F8FAFC">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tài khoản" Binding="{Binding UserName}" Width="110"/>
                            <DataGridTextColumn Header="Tên hiển thị" Binding="{Binding DisplayName}" Width="160"/>
                            <DataGridTextColumn Header="Vai trò" Binding="{Binding RoleDisplayName}" Width="150"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding StatusText}" Width="120"/>
                            <DataGridTextColumn Header="Bảo mật" Binding="{Binding SecurityNote}" Width="170"/>
                            <DataGridTextColumn Header="Sai" Binding="{Binding FailedLoginCount}" Width="50"/>
                            <DataGridTextColumn Header="Lần đăng nhập cuối" Binding="{Binding LastLoginAt}" Width="150"/>
                            <DataGridTextColumn Header="Đổi mật khẩu" Binding="{Binding PasswordChangedAt}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>

            <Border Grid.Row="4" Background="#FFFFFF" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="10">
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource Ink}"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
