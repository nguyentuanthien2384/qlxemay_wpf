<Window x:Class="QLXeMay.Windows.AIAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trợ lý AI" Height="660" Width="900" WindowStartupLocation="CenterOwner" Background="White">
    <DockPanel Margin="12">
        <TextBlock DockPanel.Dock="Top" Text="🤖 TRỢ LÝ AI QUẢN LÝ XE MÁY" FontSize="22" FontWeight="Bold" Foreground="DarkGreen" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="0,0,0,8">
            <WrapPanel HorizontalAlignment="Center">
                <Button Content="Phân tích tồn kho" Command="{Binding AnalyzeInventoryCommand}"/>
                <Button Content="Phân tích doanh thu" Command="{Binding AnalyzeRevenueCommand}"/>
                <Button Content="Phân tích khách hàng" Command="{Binding AnalyzeCustomersCommand}"/>
                <Button Content="Phân tích nhân viên" Command="{Binding AnalyzeEmployeesCommand}"/>
            </WrapPanel>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <Label Content="Nhập câu hỏi / từ khóa:"/>
                <TextBox Width="500" Text="{Binding Question, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Tư vấn SP" Command="{Binding AdviseProductCommand}"/>
                <Button Content="Tìm thông minh" Command="{Binding SmartSearchCommand}"/>
                <Button Content="Hỏi AI" Command="{Binding AskCommand}"/>
            </StackPanel>
        </StackPanel>
        <TextBox Text="{Binding Result, Mode=OneWay}" FontFamily="Consolas" FontSize="14" TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsReadOnly="True"/>
    </DockPanel>
</Window>
