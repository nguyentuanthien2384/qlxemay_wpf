<UserControl x:Class="QLXeMay.Windows.AIAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Background="#F1F5F9" FontFamily="Segoe UI">
    <UserControl.Resources>
        <SolidColorBrush x:Key="Panel" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Line" Color="#E2E8F0"/>
        <Style x:Key="Cmd" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
    </UserControl.Resources>
    <DockPanel Margin="18">
        <Border DockPanel.Dock="Top" Background="#FFFFFF" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="10" Margin="0,0,0,12">
            <TextBlock Text="TRỢ LÝ AI QUẢN LÝ XE MÁY" FontSize="22" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center"/>
        </Border>
        <Border DockPanel.Dock="Top" Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <WrapPanel HorizontalAlignment="Center">
                    <Button Content="Phân tích tồn kho" Command="{Binding AnalyzeInventoryCommand}" Style="{StaticResource Cmd}"/>
                    <Button Content="Phân tích doanh thu" Command="{Binding AnalyzeRevenueCommand}" Style="{StaticResource Cmd}"/>
                    <Button Content="Phân tích khách hàng" Command="{Binding AnalyzeCustomersCommand}" Style="{StaticResource Cmd}"/>
                    <Button Content="Phân tích nhân viên" Command="{Binding AnalyzeEmployeesCommand}" Style="{StaticResource Cmd}"/>
                </WrapPanel>
                <DockPanel Margin="0,10,0,0">
                    <Label DockPanel.Dock="Left" Content="Câu hỏi / từ khóa:"/>
                    <WrapPanel DockPanel.Dock="Right">
                        <Button Content="Tư vấn SP" Command="{Binding AdviseProductCommand}" Style="{StaticResource Cmd}" Background="#2563EB" BorderBrush="#1D4ED8" Foreground="White"/>
                        <Button Content="Tìm thông minh" Command="{Binding SmartSearchCommand}" Style="{StaticResource Cmd}"/>
                        <Button Content="Hỏi AI" Command="{Binding AskCommand}" Style="{StaticResource Cmd}"/>
                    </WrapPanel>
                    <TextBox Text="{Binding Question, UpdateSourceTrigger=PropertyChanged}" MinWidth="360"/>
                </DockPanel>
            </StackPanel>
        </Border>
        <Border Background="#F8FAFC" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="10">
            <TextBox Text="{Binding Result, Mode=OneWay}" FontFamily="Consolas" FontSize="14" TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsReadOnly="True" Background="#FFFFFF"/>
        </Border>
    </DockPanel>
</UserControl>
