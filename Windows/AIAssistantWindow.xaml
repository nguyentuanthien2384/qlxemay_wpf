<UserControl x:Class="QLXeMay.Windows.AIAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Background="{StaticResource Surface}" FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style x:Key="Cmd" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
    </UserControl.Resources>
    <DockPanel Margin="18">
        <Border DockPanel.Dock="Top" Style="{StaticResource CardBorder}" Padding="10" Margin="0,0,0,12">
            <TextBlock Text="TRỢ LÝ AI QUẢN LÝ XE MÁY" FontSize="22" FontWeight="Bold" Foreground="{StaticResource Ink}" HorizontalAlignment="Center"/>
        </Border>
        <Border DockPanel.Dock="Top" Style="{StaticResource CardBorder}" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <WrapPanel HorizontalAlignment="Center">
                    <Button Content="Phân tích tồn kho" Command="{Binding AnalyzeInventoryCommand}" Style="{StaticResource Cmd}"/>
                    <Button Content="Phân tích doanh thu" Command="{Binding AnalyzeRevenueCommand}" Style="{StaticResource Cmd}"/>
                    <Button Content="Phân tích khách hàng" Command="{Binding AnalyzeCustomersCommand}" Style="{StaticResource Cmd}"/>
                    <Button Content="Phân tích nhân viên" Command="{Binding AnalyzeEmployeesCommand}" Style="{StaticResource Cmd}"/>
                </WrapPanel>
                <DockPanel Margin="0,10,0,0">
                    <Label DockPanel.Dock="Left" Content="Câu hỏi / từ khóa:"/>
                    <WrapPanel DockPanel.Dock="Right">
                        <Button Content="Tư vấn SP" Command="{Binding AdviseProductCommand}" Style="{StaticResource AccentButton}" MinWidth="120"/>
                        <Button Content="Tìm thông minh" Command="{Binding SmartSearchCommand}" Style="{StaticResource Cmd}"/>
                        <Button Content="Hỏi AI" Command="{Binding AskCommand}" Style="{StaticResource Cmd}"/>
                    </WrapPanel>
                    <TextBox Text="{Binding Question, UpdateSourceTrigger=PropertyChanged}" MinWidth="360"/>
                </DockPanel>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardBorder}" Padding="10">
            <TextBox Text="{Binding Result, Mode=OneWay}" FontFamily="Consolas" FontSize="14" TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsReadOnly="True"/>
        </Border>
    </DockPanel>
</UserControl>
