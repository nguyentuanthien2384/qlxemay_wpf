<UserControl x:Class="QLXeMay.Windows.AIAssistantWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:infra="clr-namespace:QLXeMay.Infrastructure"
             Background="{StaticResource Surface}"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <infra:NavActiveConverter x:Key="ActiveKeyMatchConverter"/>
        <Style x:Key="AiCmd" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="118"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="12,7"/>
        </Style>
        <Style x:Key="AiQuickCmd" TargetType="{x:Type Button}" BasedOn="{StaticResource AiCmd}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="{StaticResource Card}"/>
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderStrong}"/>
            <Style.Triggers>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource ActiveKeyMatchConverter}">
                            <Binding Path="Tag" RelativeSource="{RelativeSource Self}"/>
                            <Binding Path="CurrentContextKey"/>
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <Setter Property="Background" Value="{StaticResource Accent}"/>
                    <Setter Property="Foreground" Value="{StaticResource OnAccent}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AiPrimaryCmd" TargetType="{x:Type Button}" BasedOn="{StaticResource AccentButton}">
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="118"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="12,7"/>
        </Style>
        <Style x:Key="SampleQuestionButton" TargetType="{x:Type Button}" BasedOn="{StaticResource GhostButton}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Padding="18" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="TRỢ LÝ AI PHÂN TÍCH KINH DOANH" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Ink}"/>
                <TextBlock Text="Dự báo doanh thu, cảnh báo tồn kho, tư vấn nhập hàng và gợi ý chăm sóc khách hàng dựa trên dữ liệu SQL Server của cửa hàng." Foreground="{StaticResource Muted}" Margin="0,6,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.35*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}" Padding="14">
                <StackPanel>
                    <TextBlock Text="Phân tích nhanh" FontSize="16" FontWeight="Bold" Margin="4,0,0,8"/>
                    <WrapPanel>
                        <Button Content="Tổng quan điều hành" Tag="overview" Command="{Binding ExecutiveSummaryCommand}" Style="{StaticResource AiQuickCmd}"/>
                        <Button Content="Dự báo 30 ngày" Tag="forecast" Command="{Binding ForecastRevenueCommand}" Style="{StaticResource AiQuickCmd}"/>
                        <Button Content="Gợi ý nhập hàng" Tag="reorder" Command="{Binding ReorderAdviceCommand}" Style="{StaticResource AiQuickCmd}"/>
                        <Button Content="Cảnh báo rủi ro" Tag="risk" Command="{Binding RiskWarningCommand}" Style="{StaticResource AiQuickCmd}"/>
                        <Button Content="Tồn kho" Tag="inventory" Command="{Binding AnalyzeInventoryCommand}" Style="{StaticResource AiQuickCmd}"/>
                        <Button Content="Doanh thu" Tag="revenue" Command="{Binding AnalyzeRevenueCommand}" Style="{StaticResource AiQuickCmd}"/>
                        <Button Content="Khách hàng" Tag="customers" Command="{Binding AnalyzeCustomersCommand}" Style="{StaticResource AiQuickCmd}"/>
                        <Button Content="Nhân viên" Tag="employees" Command="{Binding AnalyzeEmployeesCommand}" Style="{StaticResource AiQuickCmd}"/>
                    </WrapPanel>

                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Column="0" Grid.Row="0" Text="Câu hỏi AI" FontWeight="SemiBold" VerticalAlignment="Center" Margin="4,0,8,0"/>
                        <Grid Grid.Column="1" Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding Question, UpdateSourceTrigger=PropertyChanged}"
                                     Height="40"
                                     MinWidth="260"
                                     Margin="0,0,8,0"
                                     ToolTip="Ví dụ: Gợi ý xe dưới 40 triệu cho sinh viên"/>
                            <Button Grid.Column="1" Content="Hỏi AI" Command="{Binding AskCommand}" Style="{StaticResource AiPrimaryCmd}"/>
                        </Grid>
                        <WrapPanel Grid.Column="1" Grid.Row="1" Margin="0,8,0,0">
                            <Button Content="Tư vấn xe" Command="{Binding AdviseProductCommand}" Style="{StaticResource AiCmd}"/>
                            <Button Content="Tìm kiếm" Command="{Binding SmartSearchCommand}" Style="{StaticResource AiCmd}"/>
                            <Button Content="Làm mới" Command="{Binding ClearCommand}" Style="{StaticResource AiCmd}"/>
                        </WrapPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorder}" Padding="14">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top">
                        <TextBlock Text="Câu hỏi mẫu để demo chức năng" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="Bấm một mẫu để AI tự chạy phân tích." Foreground="{StaticResource Muted}" Margin="0,4,0,10"/>
                    </StackPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="190">
                        <ItemsControl ItemsSource="{Binding SampleQuestions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}"
                                            Command="{Binding DataContext.UseSampleQuestionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource SampleQuestionButton}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,10">
                    <Button DockPanel.Dock="Right" Content="Xem kịch bản mẫu" Command="{Binding SampleScenariosCommand}" Style="{StaticResource AiCmd}"/>
                    <StackPanel>
                        <TextBlock Text="Kết quả phân tích" FontSize="16" FontWeight="Bold"/>
                        <TextBlock Text="Kết quả được trình bày theo dạng báo cáo: KPI, nhận định, rủi ro và hành động đề xuất." Foreground="{StaticResource Muted}" Margin="0,4,0,0"/>
                        <TextBlock Text="{Binding CurrentContextLabel, StringFormat=Ngữ cảnh hiện tại: {0}}" Foreground="{StaticResource AccentPressed}" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    </StackPanel>
                </DockPanel>
                <TextBox Grid.Row="1"
                         Text="{Binding Result, Mode=OneWay}"
                         Style="{x:Null}"
                         FontFamily="Consolas"
                         FontSize="14"
                         Foreground="{StaticResource Ink}"
                         Background="{StaticResource CardAlt}"
                         BorderBrush="{StaticResource Border}"
                         BorderThickness="1"
                         Padding="14"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         IsReadOnly="True"/>
            </Grid>
        </Border>

        <Border Grid.RowSpan="3"
                Background="#66000000"
                Visibility="{Binding IsSampleScenarioPanelOpen, Converter={StaticResource BoolToVis}}"
                Panel.ZIndex="50">
            <Grid Margin="28">
                <Border MaxWidth="980"
                        MaxHeight="700"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="{StaticResource Card}"
                        BorderBrush="{StaticResource BorderStrong}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <DockPanel Grid.Row="0" Margin="0,0,0,10">
                            <Button DockPanel.Dock="Right" Content="Đóng" Command="{Binding CloseSampleScenariosCommand}" Style="{StaticResource AiCmd}"/>
                            <StackPanel>
                                <TextBlock Text="Kết quả phân tích - Xem chi tiết" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Ink}"/>
                                <TextBlock Text="Nội dung được đồng bộ với khung Kết quả phân tích bên dưới." Foreground="{StaticResource Muted}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </DockPanel>
                        <TextBox Grid.Row="1"
                                 Text="{Binding SampleScenarioDetails, Mode=OneWay}"
                                 Style="{x:Null}"
                                 FontFamily="Consolas"
                                 FontSize="13.5"
                                 Foreground="{StaticResource Ink}"
                                 Background="{StaticResource CardAlt}"
                                 BorderBrush="{StaticResource Border}"
                                 BorderThickness="1"
                                 Padding="14"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 IsReadOnly="True"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>

    </Grid>
</UserControl>
